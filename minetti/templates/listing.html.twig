{% extends 'partials/base.html.twig' %}


{% block content %}
      
  <h1>{{ page.title }}</h1>

  {{ page.content|raw }}


  <ul>
    {% for p in page.collection %}
        {% include 'partials/listing.html.twig' with {page: p} %}
    {% endfor %}
  </ul>

{% endblock %}